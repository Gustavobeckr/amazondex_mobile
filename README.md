# Iniciar o emulador pelo terminal

1º -> ' cd \Users\gustavobecker\AppData\Local\Android\Sdk\emulator'
2º -> ' .\emulator -avd emulador '

# Iniciar o React Native

```bash
# using npm
npx expo start
```
